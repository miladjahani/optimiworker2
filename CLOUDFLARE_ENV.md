# Environment

هیچ متغیر `VITE_SUPABASE_*` یا سرویس خارجی برای Database لازم نیست.

Database:
- Cloudflare D1 binding: `DB`

برای عملیات Cloudflare، توکن‌ها در D1 ذخیره و فقط توسط Worker استفاده می‌شوند.
